[package]
name = "learn-LuaInRust"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.16", features = ["derive"] }
mlua = { version = "0.9.9", features = ["lua54", "vendored"] }
